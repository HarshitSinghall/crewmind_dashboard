# Product roadmap

Validate the dashboard against a disposable staging Supabase project before production use.

Move API rate limiting to an edge or shared-store control before relying on it for abuse protection.
